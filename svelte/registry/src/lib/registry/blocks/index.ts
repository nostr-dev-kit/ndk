// @ndk-version: blocks@0.1.0
/**
 * Block Components - Preset layouts for NDK components
 *
 * Blocks are pre-composed layouts ready to use in your application.
 *
 * @example LoginCompact block:
 * ```svelte
 * <LoginCompact {ndk} onSuccess={() => console.log('Logged in!')} />
 * ```
 */

// LoginCompact block
export { default as LoginCompact } from './login-compact.svelte';
