<script lang="ts">
  import { Relay } from '$lib/registry/ui/relay';

  let value = $state<string>('');
  let error = $derived(value && !value.startsWith('wss://') && !value.startsWith('ws://') ? 'Must start with ws:// or wss://' : '');
</script>

<div class="space-y-2">
  <Relay.Input {ndk} bind:value />
  {#if error}
    <p class="text-xs text-destructive">{error}</p>
  {/if}
</div>
