import { defineConfig } from "jsrepo";
import prettier from "@jsrepo/transform-prettier"; // Optional: for formatting

export default defineConfig({
  // Registries to use
  registries: ["@nostr-dev-kit/svelte"],

  // Optional: Add transform plugins for formatting
  // Run: jsrepo config transform @jsrepo/transform-prettier
  // Or: jsrepo config transform @jsrepo/transform-biome
  transforms: [prettier()],

  // Where to install components by category
  paths: {
    "*": "$lib/ndk/components",      // Default path
    ui: "$lib/ndk/ui",               // UI primitives
    hooks: "$lib/ndk/hooks",         // Svelte hooks
    utils: "$lib/ndk/utils",         // Utilities
    icons: "$lib/ndk/icons",         // Icon components
    blocks: "$lib/ndk/blocks",       // Complete blocks
    builders: "$lib/ndk/builders",   // Component builders
  },
});
