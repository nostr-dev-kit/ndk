<script>
  import { getContext } from 'svelte';
      import { VoiceMessage } from '$lib/registry/ui/voice-message';

  const ndk = getContext<NDKSvelte>('ndk');

  // Example voice message (you would normally get this from an event)
  let voiceMessage = $state<NDKVoiceMessage | null>(null);

  // Mock voice message for demo
  $effect(() => {
    // In a real app, you'd load this from an NDK event
    // voiceMessage = NDKVoiceMessage.from(event);
  });
</script>

<div class="voice-message-demo">
  {#if voiceMessage}
    <VoiceMessage.Root {ndk} {voiceMessage}>
      <div class="player-container">
        <VoiceMessage.Player />
        <VoiceMessage.Duration />
      </div>
    </VoiceMessage.Root>
  {:else}
    <div class="placeholder">
      <p>Load a voice message event to see the player</p>
    </div>
  {/if}
</div>