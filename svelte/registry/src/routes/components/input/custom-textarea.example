<script lang="ts">
  import { UserInput } from '$lib/registry/ui/user-input';
</script>

<UserInput.Root {ndk}>
  {#snippet input({ value, onInput })}
    <textarea
      {value}
      oninput={onInput}
      placeholder="Search users..."
      class="w-full p-2 border rounded"
    />
  {/snippet}
  <UserInput.Results />
</UserInput.Root>
