<script lang="ts">
  import { Relay } from '$lib/registry/ui/relay';

  let selected = $state<string[]>([]);
</script>

<Relay.Selector.Root {ndk} bind:selected multiple={true}>
  <div class="space-y-4">
    <div>
      <h3 class="font-semibold mb-2">Select Relays</h3>
      <p class="text-sm text-muted-foreground">Choose relays from your pool</p>
    </div>

    <Relay.Selector.List compact={false} />

    {#if selected.length > 0}
      <div class="flex flex-wrap gap-2">
        {#each selected as relay}
          <span class="px-2 py-1 bg-muted rounded text-sm">{relay}</span>
        {/each}
      </div>
    {/if}
  </div>
</Relay.Selector.Root>
